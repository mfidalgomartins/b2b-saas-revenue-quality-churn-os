# Notebooks

- `01_operating_system_walkthrough.ipynb`: executive walkthrough notebook that links business framing, metric layer, scoring, scenarios, and validation outputs in one narrative.

Notebook usage:
1. Activate environment (`source .venv/bin/activate`).
2. Launch Jupyter (`jupyter lab` or `jupyter notebook`).
3. Open the notebook and run cells top-to-bottom.
